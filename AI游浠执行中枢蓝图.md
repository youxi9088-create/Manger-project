# AI 游浠执行中枢 —— 从"会说话的助手"到"能行动的 Agent"

> 版本：v1.0（蓝图阶段，未执行）
> 目标：定义 AI 游浠从"角色"到"执行"的完整架构

---

## 一、核心升级：从三层到四层

之前的架构只有"角色层"和"认知层"，现在要增加"执行层"。

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 1: 角色层（Persona）—— 已完成                        │
│  你是谁、你怎么说话、你怎么思考                             │
│  SOUL.md + MEMORY.md + USER.md + knowledge-base/            │
├─────────────────────────────────────────────────────────────┤
│  Layer 2: 认知层（Cognition）—— 已完成                      │
│  你知道什么、你能理解什么                                   │
│  knowledge-base/（项目/人员/踩坑/流程）                     │
├─────────────────────────────────────────────────────────────┤
│  Layer 3: 感知层（Perception）—— 本次新增                   │
│  你能看到什么数据、你能监控什么状态                         │
│  数据源适配 + 风险规则引擎 + 触发机制                       │
├─────────────────────────────────────────────────────────────┤
│  Layer 4: 执行层（Execution）—— 本次新增                    │
│  你能做什么操作、边界在哪里                                 │
│  三阶段执行策略 + 操作工具集 + 审批流程                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、数据源：读两个系统

### 2.1 系统 A：公司具体操作系统（外部系统）

> **待确认**：你公司的系统具体是什么？（飞书多维表格 / PMS / OA / 自研系统？）
> 在确认之前，我按通用接口设计，后续只需适配具体协议。

**需要读取的数据**：
| 数据类型 | 用途 | 获取方式 |
|---------|------|---------|
| 任务列表 | 看有哪些待办、是否超期 | API 或数据库直连 |
| 任务详情 | 负责人、deadline、进度、状态 | API 或数据库直连 |
| 项目里程碑 | 项目整体进度 | API 或数据库直连 |
| 人员负载 | 谁在忙什么、是否过载 | API 或数据库直连 |

**接入方案**（三选一，你决定）：
- **方案1：API 直连**（推荐）—— 如果系统有开放 API
- **方案2：数据库只读** —— 如果系统用 MySQL/PostgreSQL 且你有只读权限
- **方案3：飞书多维表同步** —— 如果系统数据已同步到飞书

### 2.2 系统 B：OpenClaw（自有系统）

OpenClaw 目前已有的数据能力：

| 子系统 | 现有能力 | 可用于风险识别 |
|--------|---------|--------------|
| **IM Chat Analyzer** | 群聊记录、分析报告、会议纪要 | ✅ 从聊天记录识别"可能要延期"的苗头 |
| **Claw Studio** | 每日任务、事务列表 | ✅ 你的个人待办状态 |
| **MCP** | 飞书-OA自动下单、任务队列 | ✅ 任务执行状态、字段映射 |
| **会议助手** | 会议音频、ASR纪要 | ✅ 从会议纪要提取决策和风险 |

**需要补充的数据接口**：

目前 OpenClaw 各子系统之间数据是割裂的，需要统一一个"AI游浠数据聚合层"：

```
公司系统 API          OpenClaw 子系统
     │                      │
     └──────────┬───────────┘
                ↓
    ┌───────────────────────────────┐
    │  AI游浠数据聚合层              │
    │  （统一数据模型 + 定时同步）    │
    │                              │
    │  统一数据模型：               │
    │  - task: {id, title, assignee, deadline, status, source_system, risk_level}
    │  - project: {id, name, milestone, progress, health}
    │  - person: {id, name, role, workload, current_tasks}
    │  - risk: {id, type, severity, source, detected_at, related_tasks}
    └───────────────────────────────┘
                ↓
    ┌───────────────────────────────┐
    │  AI游浠执行中枢（Agent）       │
    │                              │
    │  • 风险扫描引擎               │
    │  • 规则判断引擎               │
    │  • 方案生成引擎               │
    │  • 执行策略引擎               │
    └───────────────────────────────┘
```

---

## 三、风险规则引擎（你定义，支持迭代）

### 3.1 规则设计原则

- **规则由你定义**：不是 AI 自己猜什么算风险
- **规则支持热更新**：改规则不需要重启系统
- **规则分等级**：不同严重度对应不同响应方式
- **规则可组合**：多个条件同时满足时触发

### 3.2 规则配置文件

```yaml
# ~/.hermes/knowledge-base/config/risk-rules.yaml
# 风险规则配置（你随时可编辑）

rules:
  # ── 规则1：任务超期 ──
  - id: task-overdue
    name: "任务已超期"
    severity: P1      # P0=致命, P1=严重, P2=警告, P3=提示
    source: [company_system, openclaw]
    conditions:
      - field: deadline
        operator: "<"
        value: "now()"
      - field: status
        operator: "!="
        value: "已完成"
    action:
      - type: "notify"
        message: "任务 {title} 已超期 {overdue_days} 天"
      - type: "suggest"
        options: ["延期", "转交", "取消", "忽略"]

  # ── 规则2：deadline 临近 ──
  - id: deadline-approaching
    name: "deadline 临近"
    severity: P2
    source: [company_system, openclaw]
    conditions:
      - field: deadline
        operator: "between"
        value: ["now()", "now() + 3 days"]
      - field: status
        operator: "in"
        value: ["进行中", "待开始"]
    action:
      - type: "notify"
        message: "任务 {title} 将在 {days_left} 天后到期"
      - type: "suggest"
        options: ["催办", "延期", "资源支持"]

  # ── 规则3：人员过载 ──
  - id: person-overloaded
    name: "人员负载过高"
    severity: P2
    source: [company_system]
    conditions:
      - field: person.active_tasks
        operator: ">"
        value: 5
      - field: person.high_priority_tasks
        operator: ">"
        value: 2
    action:
      - type: "notify"
        message: "{person_name} 当前有 {task_count} 个活跃任务，其中 {high_count} 个高优先级"
      - type: "suggest"
        options: ["任务重新分配", "延期低优先级", "增加人力"]

  # ── 规则4：项目里程碑风险 ──
  - id: milestone-at-risk
    name: "里程碑可能无法达成"
    severity: P1
    source: [company_system]
    conditions:
      - field: milestone.days_left
        operator: "<"
        value: 7
      - field: milestone.progress
        operator: "<"
        value: 0.7
    action:
      - type: "notify"
        message: "项目 {project} 的里程碑 {milestone} 距 deadline 只剩 {days} 天，但进度仅 {progress}%"
      - type: "suggest"
        options: ["砍范围", "延期", "加人", "拉会对齐"]

  # ── 规则5：踩坑模式匹配 ──
  - id: pattern-repeat-risk
    name: "历史踩坑模式可能重演"
    severity: P2
    source: [knowledge_base]
    conditions:
      - field: task.type
        operator: "=="
        value: "外包"
      - field: task.deadline_confirmed
        operator: "=="
        value: false
    action:
      - type: "notify"
        message: "外包任务 {title} 未确认 deadline，历史上类似情况导致延期"
      - type: "reference"
        knowledge: "insights/lessons-learned/外包踩坑记录.md"
      - type: "suggest"
        options: ["立即确认 deadline", "要求书面排期", "启动小单试点"]

  # ── 规则6：群聊风险信号 ──
  - id: chat-risk-signal
    name: "群聊中出现风险信号"
    severity: P2
    source: [im_analyzer]
    conditions:
      - field: chat.content
        operator: "contains_any"
        value: ["延期", "来不及", "风险", "blocker", "卡住了", "搞不定"]
      - field: chat.sender_role
        operator: "in"
        value: ["技术主程", "项目负责人"]
    action:
      - type: "notify"
        message: "{group} 群中 {sender} 提到：{snippet}"
      - type: "suggest"
        options: ["主动询问", "拉会对齐", "升级处理"]

  # ── 规则7：会议决策未跟进 ──
  - id: meeting-action-pending
    name: "会议待办未按时完成"
    severity: P2
    source: [meeting_assistant]
    conditions:
      - field: meeting_action.deadline
        operator: "<"
        value: "now()"
      - field: meeting_action.status
        operator: "=="
        value: "未完成"
    action:
      - type: "notify"
        message: "会议 {meeting_title} 的待办 {action} 已超期，负责人 {assignee}"
      - type: "suggest"
        options: ["催促负责人", "重新分配", "延期"]
```

### 3.3 规则的迭代方式

```
你遇到新情况 → "帮我加个风险规则：当XXX时，提醒YYY"
    ↓
AI：在 risk-rules.yaml 中新增一条规则
    ↓
你确认 → 规则立即生效（下次扫描时生效）
    ↓
运行一段时间后 → 你觉得规则太吵/不够敏感
    ↓
你调整阈值 → 规则热更新
```

---

## 四、三阶段执行边界

### 4.1 阶段定义

| 阶段 | 名称 | 聊天回复 | 风险建议 | 工具操作 | 通知他人 | 状态 |
|------|------|---------|---------|---------|---------|------|
| **阶段1** | 建议模式 | ✅ 人工触发 | ✅ 主动报告 | ❌ 不给建议 | ❌ | **当前目标** |
| **阶段2** | 半自动模式 | ✅ AI自动 | ✅ 主动报告 | ✅ 给建议+草稿 | ✅ 给建议 | 未来 |
| **阶段3** | 全自动模式 | ✅ AI自动 | ✅ 主动报告 | ✅ 执行 | ✅ 执行 | 远期 |

### 4.2 各阶段详解

#### 阶段1：建议模式（当前目标）

**聊天回复**：
- 你发起对话 → AI游浠回复
- 回复基于知识库 + 实时数据

**风险识别**：
- AI 定时扫描或你手动触发
- 发现风险 → 生成报告 → 呈现给你
- **你决定**怎么处理

**操作边界**：
```
AI游浠可以：
  ✅ 读任务状态
  ✅ 分析风险
  ✅ 给出建议（"建议延期3天"）
  ✅ 生成操作草稿（"如果延期，我会这样操作：..."）
  ✅ 回答你关于任何任务的询问

AI游浠不可以：
  ❌ 直接改任务状态
  ❌ 直接@别人
  ❌ 直接发通知
  ❌ 自动执行任务

你的操作：
  👤 看AI的分析
  👤 决定接受/修改/拒绝建议
  👤 手动执行操作（或在AI指导下点几下）
```

**阶段1 的交互示例**：

```
【定时触发，每天早上9点】

AI游浠："早上好。今日风险扫描结果：

🔴 P1 严重（1项）：
  • 任务'实验颗粒API联调'已超期2天（deadline: 6/5）
    建议：确认当前进度，决定延期或升级
    操作草稿：[延期3天] [转交李明] [忽略]

🟡 P2 警告（2项）：
  • 任务'美术史UI走查'将在2天后到期，进度60%
    建议：催促QA加快速度，或评估是否需要延期
  • 外包任务'鸭科夫角色模型'未书面确认deadline
    建议：立即要求外包方书面确认排期
    历史参考：[外包踩坑记录]

请回复：1.接受建议 2.修改 3.忽略"

你："实验颗粒那个，帮我延期3天"

AI游浠："好的，延期操作方案：
  系统：公司PMS
  任务：实验颗粒API联调
  操作：延期3天，新deadline 6/11
  备注：'联调进度受阻，延期3天确保质量'
  
  请在PMS中执行以下操作：[截图/步骤]
  需要我帮你生成给团队的同步消息吗？"
```

#### 阶段2：半自动模式（未来）

在阶段1稳定运行后，逐步开放：

```
AI游浠新增能力：
  ✅ 自动生成操作草稿（消息、邮件、评论）
  ✅ 预览给你看（"我准备这样发，你看一下"）
  ✅ 你说"发" → AI帮你发
  ✅ 可以@人，但需要你先确认@谁、说什么

你的操作：
  👤 从"手动执行"变成"点确认"
```

**阶段2 的交互示例**：

```
你："实验颗粒那个帮我延期3天"

AI游浠："好的，我已完成以下操作：

  [系统] 公司PMS
  [任务] 实验颗粒API联调 → 延期至 6/11
  [备注] '联调进度受阻，延期3天确保质量'

  [消息] 已生成团队同步消息：
  '@何松航 @李明 实验颗粒API联调延期至6/11，
   原因是联调进度受阻，确保质量。有异议请提出。'

  [操作] 请确认是否发送消息？ [发送] [修改] [不发]"

你："发"

AI游浠："已发送。消息记录已保存到知识库。"
```

#### 阶段3：全自动模式（远期）

在阶段2稳定运行后，配置审批规则：

```
审批规则（你定义）：
  • P3 提示 → AI自动处理，无需审批
  • P2 警告 → AI处理，事后通知你
  • P1 严重 → AI给出方案，你点确认后执行
  • P0 致命 → AI只报告，由你决策

AI游浠新增能力：
  ✅ P3 自动延期/自动催办
  ✅ P2 自动发通知，事后汇报
  ✅ P1 预执行，等你点确认

你的操作：
  👤 从"点确认"变成"事后审查"
```

---

## 五、多模式触发机制

### 5.1 触发方式总览

```
┌─────────────────────────────────────────────────────────────┐
│                     触发方式矩阵                             │
├─────────────────────────────────────────────────────────────┤
│  定时触发      │  手动触发        │  被动触发（事件驱动）    │
├─────────────────────────────────────────────────────────────┤
│  • 每日晨报    │  • "检查风险"    │  • 任务deadline变更      │
│  • 每周周报    │  • "看看李明     │  • 新任务分配            │
│  • 每30分钟    │    的任务"       │  • 群聊出现风险关键词      │
│    巡检        │  • "分析本周     │  • 会议待办超期          │
│                │    风险"         │  • 系统告警              │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 定时触发（Cron）

```yaml
# ~/.hermes/knowledge-base/config/triggers.yaml

triggers:
  # ── 每日晨报 ──
  - id: daily-morning-report
    type: cron
    schedule: "0 9 * * 1-5"    # 工作日 9:00
    action: scan_risks
    params:
      severity_min: P2          # 只报 P2 及以上
      output_format: summary    # 摘要格式

  # ── 每日晚报 ──
  - id: daily-evening-report
    type: cron
    schedule: "0 19 * * 1-5"   # 工作日 19:00
    action: scan_risks
    params:
      severity_min: P2
      output_format: detailed   # 详细格式

  # ── 高频巡检（阶段3启用）──
  - id: frequent-check
    type: cron
    schedule: "*/30 * * * *"   # 每30分钟
    action: scan_risks
    params:
      severity_min: P0          # 只报 P0（致命）
      auto_notify: true         # 自动通知

  # ── 每周深度分析 ──
  - id: weekly-analysis
    type: cron
    schedule: "0 10 * * 1"     # 每周一 10:00
    action: weekly_report
    params:
      include_trends: true      # 包含趋势分析
      include_recommendations: true  # 包含建议
```

### 5.3 手动触发

```
你主动发起：

【场景1】全面扫描
你："检查一下当前所有风险"
AI：执行全量扫描 → 生成报告

【场景2】聚焦扫描
你："看看 OpenQ 项目有什么风险"
AI：只扫描 OpenQ 相关任务 → 生成报告

【场景3】人员扫描
你："李明最近忙什么？有风险吗？"
AI：扫描李明的任务负载和进度 → 生成报告

【场景4】历史对比
你："这周和上周比，风险有什么变化？"
AI：对比两周的风险数据 → 趋势报告
```

### 5.4 被动触发（事件驱动）

```
系统事件 → 自动触发扫描

【事件1】任务状态变更
公司系统：某任务从"进行中"变成"已阻塞"
AI游浠：立即扫描该任务关联的其他任务
        → 如果发现连锁风险 → 报告给你

【事件2】deadline 变更
公司系统：某项目 deadline 从 6/30 改成 6/20
AI游浠：立即重新计算所有依赖任务的风险
        → 发现3个任务可能因此超期 → 报告

【事件3】群聊风险信号
IM Analyzer：检测到群聊中出现"延期""来不及"等词
AI游浠：结合任务数据判断
        → 如果说话人有关键任务即将到期 → 报告

【事件4】会议待办超期
会议助手：某会议待办 deadline 已过
AI游浠：检查负责人当前状态
        → 如果负责人任务过载 → 建议重新分配

【事件5】系统告警
公司系统：服务器/构建失败
AI游浠：判断影响范围
        → 如果影响关键路径 → 报告
```

---

## 六、与现有 hermes 的集成方式

### 6.1 架构关系

```
hermes（AI游浠Agent）
    │
    ├─→ Layer 1: 角色层（SOUL.md / MEMORY.md / USER.md）
    │        已就绪 ✅
    │
    ├─→ Layer 2: 认知层（knowledge-base/）
    │        已就绪 ✅
    │
    ├─→ Layer 3: 感知层（新增）
    │        ├─ 数据源适配器
    │        │   ├─ 公司系统适配器（待开发）
    │        │   └─ OpenClaw 适配器（待开发）
    │        ├─ 风险规则引擎（risk-rules.yaml）
    │        └─ 触发机制（triggers.yaml + 事件监听）
    │
    └─→ Layer 4: 执行层（新增）
             ├─ 三阶段策略配置
             ├─ 操作工具集（读写API）
             └─ 审批流程
```

### 6.2 hermes 中的 Skill 设计

为了让 hermes 能够调用这些能力，需要创建以下 Skills：

```
~/.hermes/skills/
├── risk-scanner/           ← 风险扫描 Skill
│   └── SKILL.md
│       "当用户要求检查风险时，调用风险扫描API，
│        读取 risk-rules.yaml 中的规则，返回风险报告"
│
├── task-operator/          ← 任务操作 Skill（阶段2/3）
│   └── SKILL.md
│       "当用户要求操作任务时，根据当前阶段策略，
│        给出建议或执行操作"
│
├── project-health/         ← 项目健康度 Skill
│   └── SKILL.md
│       "分析项目整体健康度，包括进度、风险、资源"
│
└── notification-drafter/   ← 通知草稿 Skill（阶段2）
    └── SKILL.md
        "根据场景生成通知消息草稿，供用户确认后发送"
```

### 6.3 数据流

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   公司系统    │     │   OpenClaw   │     │  knowledge-  │
│              │     │   子系统      │     │   base/      │
└──────┬───────┘     └──────┬───────┘     └──────┬───────┘
       │                    │                    │
       └────────────────────┼────────────────────┘
                            ↓
                   ┌─────────────────┐
                   │ 数据聚合层        │
                   │ （定时同步/实时） │
                   └────────┬────────┘
                            ↓
                   ┌─────────────────┐
                   │ 风险扫描引擎      │
                   │ （读取规则配置）   │
                   └────────┬────────┘
                            ↓
                   ┌─────────────────┐
                   │ 触发器判断        │
                   │ （定时/手动/事件） │
                   └────────┬────────┘
                            ↓
                   ┌─────────────────┐
                   │ 方案生成 + 执行  │
                   │ （按阶段策略）    │
                   └────────┬────────┘
                            ↓
                   ┌─────────────────┐
                   │  hermes Agent   │
                   │  （AI游浠）      │
                   └─────────────────┘
```

---

## 七、实施路线图

### Phase 1：基础设施（1-2周）

| 任务 | 说明 | 优先级 |
|------|------|--------|
| 确认公司系统接口 | 你告诉我公司系统是什么，怎么接 | P0 |
| 设计统一数据模型 | 定义 task/project/person/risk 的标准结构 | P0 |
| 创建 OpenClaw 数据适配器 | 读现有子系统的数据 | P1 |
| 创建风险规则配置框架 | risk-rules.yaml 框架 + 解析器 | P1 |
| 创建触发机制框架 | triggers.yaml + cron + 事件监听 | P1 |

### Phase 2：阶段1 上线（2-3周）

| 任务 | 说明 | 优先级 |
|------|------|--------|
| 风险扫描引擎 | 基于规则扫描两个系统的数据 | P0 |
| 定时晨报/晚报 | 每天早上9点、晚上7点生成报告 | P0 |
| 手动扫描命令 | "检查风险" → 触发扫描 | P0 |
| 知识库联动 | 扫描结果关联踩坑记录、人员档案 | P1 |
| hermes Skill 封装 | risk-scanner / project-health | P1 |

### Phase 3：阶段2 准备（3-4周）

| 任务 | 说明 | 优先级 |
|------|------|--------|
| 操作草稿生成 | AI生成延期/催办消息草稿 | P1 |
| 预览+确认流程 | 给你看草稿，你点确认才执行 | P1 |
| 消息发送集成 | 连接飞书/99U/邮件API | P2 |
| 半自动审批流 | 低危操作自动，高危操作确认 | P2 |

### Phase 4：阶段3 探索（远期）

| 任务 | 说明 | 优先级 |
|------|------|--------|
| 全自动审批规则 | 你定义哪些操作可以全自动 | P3 |
| 被动触发完善 | 更多事件类型的监听 | P3 |
| 预测性风险 | 基于历史数据预测未来风险 | P3 |

---

## 八、下一步：需要你确认的事项

### 必须确认（影响Phase 1）

1. **公司系统是什么？**
   - 系统名称？
   - 有没有API？还是只能读数据库？
   - 数据格式大概什么样？

2. **风险规则的初版**
   - 上面我写的7条规则，哪些对你有用？
   - 还需要什么规则？
   - 优先级和阈值你觉得合理吗？

3. **触发方式**
   - 定时：每天9点+19点的晨报晚报，OK吗？
   - 手动：你希望用什么命令触发？（"检查风险" / "扫描" / 其他？）

### 可以后续细化

4. **阶段演进节奏**
   - 阶段1跑多久你觉得可以进阶段2？
   - 有没有特别想提前的能力？

5. **通知渠道**
   - 风险报告发到哪里？（hermes对话 / 飞书 / 邮件？）

---

> 这份蓝图目前是 v1.0，完全基于你的需求设计，还没有开始写代码。
> 你把上面的问题确认后，我们再进入 Phase 1 的具体实施。
