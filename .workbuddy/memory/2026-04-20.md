# 2026-04-20 工作日志

## 双 Agent 系统实施方案制定

完成了 OpenClaw 双 Agent 系统（主 Agent + 执行 Agent）的详细实施方案：

- **架构**：主 Agent（PM Agent/Orchestrator）+ 执行 Agent，基于 Moonshot Function Calling 直连
- **核心设计**：自研轻量 Agent Loop 引擎（~150行），工具注册表（Map 结构），14 个内置工具
- **新增文件**：7 个（agent-tools.ts / agent-loop.ts / agent-prompts.ts / exec-agent.ts / agent-main.ts / agent/page.tsx / agent.ts types）
- **修改文件**：3 个（index.ts 注册路由 / db.ts 新表 / app-sidebar.tsx 导航）
- **不改动任何现有逻辑**，纯新增模块
- **数据库新增**：agent_tasks 表（执行任务追踪）
- **预估工时**：约 9 小时
- 方案产物保存在 artifact 目录：agent-implementation-plan.md
- 输出了给编程智能体执行的详细改造计划：agent-coding-task.md，后续整合了 UI 需求更新为完整版（共 14 个步骤）
- UI 补充：修复 employees/page.tsx Dialog Bug（第 705 行 JSX 在 return 之外）、新增 PM Agent 对话页、ToolCallCard + ExecAgentCard 组件、侧边栏新增 AI Agent 入口、员工卡片新增"对话"按钮
- 核对 artifact 文档 `agent-coding-task.md` 后确认：7 条审核意见尚未完整落文，当前仍残留 Step 7 路由过重、dispatch_exec_agent 假执行、Step 11 硬编码 SERVER、Step 12 锚点风险、Step 3 工具文件未默认拆分等问题。
- 已将 `agent-coding-task.md` 修订为最终执行版：新增 `agent-chat-service.ts`、将工具层默认拆分为 query/write/registry 三文件、在 `agent-loop.ts` 中引入 `overrideExecutor` 真正调度执行 Agent、前端改为用统一 API base 或 `NEXT_PUBLIC_API_URL`、SSE 增加 `tool_call_id` 用于前端唯一匹配。
- 在文档末尾追加 Phase 2 附录（Hermes Skill 注入方案）：定位为执行 Agent system prompt 增强，注入时机为主体改造完成后；包含 skill-loader.ts、3 个初始 SKILL.md（ue5-dev / pm-workflow / im-analysis）及完整验收 Checklist。

## 双 Agent 系统改造完成（2026-04-20 16:47-17:xx）

严格按 agent-coding-task.md 执行，已创建/修改以下文件：

**新增后端（8 个）**
- packages/shared/types/agent.ts（AgentSSEEvent 含 tool_call_id）
- packages/server/src/services/agent-tools.ts
- packages/server/src/services/agent-tools-query.ts（9 个只读工具）
- packages/server/src/services/agent-tools-write.ts（5 个写工具）
- packages/server/src/services/agent-prompts.ts
- packages/server/src/services/agent-loop.ts（含 overrideExecutor）
- packages/server/src/services/exec-agent.ts
- packages/server/src/services/agent-chat-service.ts（dispatch_exec_agent 真调度）
- packages/server/src/routes/agent-main.ts（轻量路由）

**修改后端（3 个）**
- db.ts：追加 agent_tasks 表
- index.ts：注册 agentMainRoutes
- shared/types/index.ts：追加 agent.ts 导出

**新增前端（4 个）**
- packages/web/src/components/agent/ToolCallCard.tsx
- packages/web/src/components/agent/ExecAgentCard.tsx
- packages/web/src/app/tools/agent/page.tsx（PM Agent 对话页）

**修改前端（2 个）**
- employees/page.tsx：修复 Dialog Bug（移入 return 内）+ 新增"对话"按钮（MessageSquare）
- app-sidebar.tsx：新增 AI Agent 入口

**文档（1 个）**
- PROJECT_STRUCTURE.md 更新

## Agent 可视化改造计划制定（18:50）

输出 3 需求的改造方案 `agent-visual-refactor-plan.md`：
1. 旧角色模板（3 个虚设角色）→ 映射为 2 个真实 Agent（main/exec）的运行态
2. 员工列表增加 Agent 特性/能力/工具/Skill/状态展示
3. 看板新增 Agent 任务面板（正在执行/历史完成/待执行）
预估 3 小时，修改 5 个文件，不新建文件



